Write and Test Assembly Language Programs that:

1. Read two values A and B from user.

   If A > B
	 C = A - B
   else 
	 C = B - A
	
	print C

2. Read a value N from user.

   Compute and print the total of N more 
     values read from user.
   
3. Create an array of int values

   Print the sum of the first and last 
     values in the array